server:
  port: 1016

spring:
  datasource:
    url: jdbc:postgresql://localhost:5438/bank?currentSchema=exchange
    username: yandex
    password: yandex
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        default_schema: exchange
  thymeleaf:
    encoding: UTF-8
  liquibase:
    change-log: classpath:/db/changelog/rootChangeLog.xml
    liquibase-schema: public
    default-schema: exchange
    parameters:
      schemaName: exchange
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5438/bank?currentSchema=exchange
    user: yandex
    password: yandex
management:
  endpoint:
    health:
      probes:
        enabled: true
      show-details: always
    liquibase:
      access: read_only
  endpoints:
    web:
      exposure:
        include: "*"
logging:
  level:
    org.springframework.jdbc.core.JdbcTemplate: DEBUG
    org.springframework.web: DEBUG
    org.example: DEBUG
    liquibase: INFO

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/